import 'package:flutter/material.dart';

class TasksWriter extends StatelessWidget {
  const TasksWriter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
