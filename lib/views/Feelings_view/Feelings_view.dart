import 'package:flutter/material.dart';

class FeelingsWriter extends StatelessWidget {
  const FeelingsWriter({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
        child: Scaffold(
          body: Column(
            children: [
              AppBar(),
            ],
          ),
        ));
  }
}
